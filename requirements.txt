# ── Core ──────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
sqlmodel>=0.0.19
celery[redis]>=5.4.0
flower>=2.0.1
redis>=5.0.0
httpx>=0.27.0
python-multipart>=0.0.9
websockets>=13.0

# ── PyTorch (CUDA 12.x for A100) ─────────────────────────────────
# Install separately: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
torchvision>=0.19.0

# ── Diffusers / HuggingFace ──────────────────────────────────────
diffusers>=0.32.0
transformers>=4.45.0
accelerate>=0.34.0
huggingface_hub>=0.25.0
sentencepiece>=0.2.0
tokenizers>=0.20.0
tiktoken>=0.7.0
protobuf>=4.25.0
safetensors>=0.4.4

# ── Flash Attention (A100 native) ────────────────────────────────
# Install separately: pip install flash-attn --no-build-isolation
xformers>=0.0.28

# ── Audio ────────────────────────────────────────────────────────
kokoro>=0.9.4
soundfile>=0.12.1
librosa>=0.10.2
numpy>=1.26.0
scipy>=1.13.0
# elevenlabs — removed, Kokoro handles TTS locally for free

# ── LLM / Prompts ────────────────────────────────────────────────
anthropic>=0.34.0

# ── NLP (scene splitting) ────────────────────────────────────────
nltk>=3.9.0

# ── Config ───────────────────────────────────────────────────────
pyyaml>=6.0.2
python-dotenv>=1.0.1

# ── Utilities ────────────────────────────────────────────────────
tqdm>=4.66.0
rich>=13.8.0
psutil>=6.0.0

# ── Video export (required by diffusers export_to_video) ─────────
imageio>=2.34.0
imageio-ffmpeg>=0.5.0
opencv-python-headless>=4.9.0
